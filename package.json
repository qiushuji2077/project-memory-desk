{
  "name": "project-memory-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first project memory desk for keeping final memory and memory flow together.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run build"
  },
  "keywords": [
    "local-first",
    "project-memory",
    "knowledge-work",
    "client-delivery"
  ],
  "license": "MIT",
  "dependencies": {
    "vite": "^7.1.0"
  },
  "devDependencies": {}
}
